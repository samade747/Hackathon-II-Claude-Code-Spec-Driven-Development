# Task 001 — Implement Phase I Console Todo App

## Objective
Implement all Phase I features for the Todo console app according to:

- constitution/CONSTITUTION.md
- spec/phase-1-spec.md
- spec/test-cases.md

## Subtasks

1. Implement `Task` dataclass in `src/todo/models.py`.
2. Implement utilities in `src/todo/utils.py`.
3. Implement in-memory store in `src/todo/storage.py`.
4. Implement CLI with subcommands in `src/todo/cli.py`.
5. Implement tests in `tests/test_todo.py`.
6. Ensure all tests pass with `pytest -q`.

## Acceptance Criteria
- All tests in `tests/test_todo.py` pass.
- CLI commands behave as documented in `spec/phase-1-spec.md`.
